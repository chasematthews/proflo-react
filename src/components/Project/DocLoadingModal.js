import React from "react"
import styles from '@styles/Project.module.css';

const DocLoadingModal = () => {

    return (
        <div className={styles.docLoadModal}>
            Loading Document. Please Wait a Moment.
        </div>
    )
}

export default DocLoadingModal